import React from "react";
import { Link } from "react-router-dom";

const Home = () => {
  // add button <loginig> <register>

  return (
    <div className=" v-screen h-screen flex justify-center flex-row"></div>
  );
};

export default Home;
